# Sitio generado por MkDocs (regenerable con `mkdocs build` / `mkdocs gh-deploy`)
site/

# Entorno virtual de Python
.venv/
venv/

# Cachés
__pycache__/
.cache/
*.pyc

# Sistema
.DS_Store
Thumbs.db

# Material privado (no distribuir): reglamento original, traducción literal,
# tooling de build y PDFs generados. (Antes se llamaba personal/.)
Squad_Leader_4th_Edition_Rules.pdf
reglamento/
